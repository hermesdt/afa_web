<%= form_for(@field_category) do |f| %>
  <% if @field_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@field_category.errors.count, "error") %> prohibited this field_category from being saved:</h2>

      <ul>
      <% @field_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
